To run this code you need to:

1) Install the Windows Azure SDK for Node.js. You can download it from https://www.windowsazure.com/en-us/develop/nodejs/
2) Install the socket.io module inside the server-worker project.
3) Add the node.exe file inside the the server-worker project.